# Delphos final-user runtime requirements.
#
# R is also required because Delphos estimates proposed models through the
# Apollo backend. See requirements-r.txt and install_r_requirements.R.

torch>=2.8,<2.9
numpy>=1.26,<3.0
pandas>=2.2,<3.0
pyyaml>=6.0,<7.0
rpy2>=3.5,<4.0
tqdm>=4.66,<5.0
